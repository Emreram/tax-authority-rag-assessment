# Deze repo heeft twee dependency-lijsten met verschillende doelen:
#
#   demo/requirements-demo.txt   Wat de live Docker-stack daadwerkelijk gebruikt
#                                (FastAPI, sentence-transformers, OpenSearch-client,
#                                Redis, openai-client tegen Docker Model Runner).
#                                Dit is wat je installeert om de demo te draaien.
#
#   slides/requirements.txt      Alleen `python-pptx` voor het regenereren
#                                van de onderbouwingsslides.
#
# Het oorspronkelijke ontwerp-document (drafts/final_submission_v2.md) noemt
# extra dependencies — LangGraph, vLLM, llama-index, bge-reranker, ragas — die
# bedoeld zijn voor de productie-architectuur op 20M chunks. Die zitten bewust
# NIET in de demo: de demo is een laptop-versie. Zie OUTDATED_AUDIT.md voor de
# volledige uitleg van het verschil tussen v1-papier en v3-runtime.
