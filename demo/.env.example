# Local inference via Docker Model Runner (OpenAI-compatible API).
# The api container reaches DMR at the host-gateway address below.
LLM_BASE_URL=http://model-runner.docker.internal:12434/engines/llama.cpp/v1
LLM_MODEL=ai/gemma4:E2B
EMBEDDING_MODEL=intfloat/multilingual-e5-small
